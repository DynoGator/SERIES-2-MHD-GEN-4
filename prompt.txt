<USER_REQUEST>\n# SERIES-2-MHD-GEN-4 — Phase 2 Completion & Integrity Correction\n### Agent execution prompt (Antigravity CLI / autonomous coding agent)\n**Working tree:** `/home/dynogator/Desktop/claudehome/2mhd_digital_twin/`\n**Branch:** continue on `twin/phase2-physical-accuracy` (PR #9 is open into `main` — do NOT merge until this prompt's Definition of Done is met).\n\n---\n\n## 0. Why you are back here — read before touching anything\n\nAn independent audit ran your pushed branch. Phase 2 is **not** done, and the closing report contained false statements. This is not a criticism exercise; it is a correction. Ground truth:\n\n- **`origin/main` is still at `361d55f`.** PR #9 is open, **not merged**. Do not say \"merged\" again unless `git ls-remote origin refs/heads/main` shows this branch's HEAD.\n- **The suite is red.** Your own PR body said `7 failed`. Confirmed real failures include `tests/physics/test_accumulator.py::test_pressure_relaxation` and `tests/unit/test_state_vector.py::test_segment_array_round_trip`.\n- **You reported a fix that is not in the repo.** You claimed \"updated tests to expect dV/dt < 0,\" but that edit was lost in your rebase/stash and never committed. The pushed test still asserts `dV > 0`.\n- **The P1 accumulator \"fix\" is a fudge**, not physics (see P-A).\n- **The P2 self-test suite does not test what it was supposed to** (see P-D).\n\n**Three laws still in force, plus one new one:**\n- **L-A:** When the harness says the twin is non-physical, fix the twin. Never silence the harness — and never insert a term whose only justification is to keep a state variable in range.\n- **L-B:** `pytest.raises(Exception)` (bare or in a tuple) is banned. Assert the specific type and reason.\n- **L-C:** Unlabeled red trains you to ignore red. Known-failing is acceptable only as `xfail(strict=True, reason=…)`.\n- **L-D (NEW): Report only what the pushed tree actually contains, verified by re-reading it.** After your final push, re-clone or `git show HEAD:<file>` to confirm every claim. A rebase can silently drop a commit; if you say you changed a file, prove it from the remote.\n\nDo the priorities in order. **After each, run the full suite and paste the real `pytest -rf -q` tail.** Commit per priority. Do not advance on an unverified suite.\n\n---\n\n## P-A — Remove the accumulator fudge; make the model physical or let it terminate honestly  ⚠️ FIRST\n\n`physics/thermo/accumulator.py` currently keeps `V_accum` positive with a hand-tuned restoring force:\n```python\nrefill_rate = 0.0\nif state.V_accum > self.V_pre:   refill_rate = -1e-4 * (state.V_accum - self.V_pre)\nelif state.V_accum < 0.05:       refill_rate = 1e-4 * (0.05 - state.V_accum)\ndV_dt += refill_rate\n```\nThis is a spring to a magic band `[0.05, V_pre]` with an invented gain. It violates L-A. It also still has `max(V_accum, 1e-6)` clamps inside `pressure()` and `work_recovered()`, and arbitrary constants `A_piston = 0.1 # arbitrary`, `m_eff = 10.0 # arbitrary`, hardcoded `C_v = 1e-6`, plus dead variables and stream-of-consciousness comments.\n\n**Do:**\n1. **Delete** the `refill_rate` block entirely. **Delete** the `max(..., 1e-6)` clamps inside `pressure()` and `work_recovered()` (the de-clamp doctrine applies at the module level too — the state substrate must be free to go non-physical so the harness can catch it).\n2. **Delete** the dead `A_piston`/`m_eff` variables and the `# But wait!…` narration comments. Physics modules contain physics, not deliberation.\n3. Decide the accumulator's real behavior — pick **one** and implement it honestly:\n   - **(a) Real recharge:** model a compressor/return path driven by `ControlVector.compressor_rpm_cmd` with a defined pump characteristic (mass-flow vs Δp). The recharge rate must be a function of a **control input and a physical curve**, not of \"how far V_accum is from a setpoint.\"\n   - **(b) Honest saturation:** if no recharge exists in this operating mode, the accumulator legitimately bottoms out. Then the run must **terminate on the `V_accum` WARNING event** you added in P1 (verify it actually fires — see acceptance) with reason `ACCUMULATOR_NEAR_EMPTY`, and the nominal-run test asserts \"completes for full `t_end` **or** terminates on the WARNING event with that reason.\" A clean saturation stop is a valid physical result; a spring is not.\n4. Note the likely root cause for the reviewer: `dp_vessel_dt = k_stiffness*(p_target - p_vessel)` with a high `p_target` drives `p_vessel` up, pushing fluid in and compressing the gas toward zero with no relief. The physical fix is coupled vessel↔accumulator mass conservation and/or a relief path — or option (b). Either way, **no term exists to keep V_accum in range for its own sake.**\n\n**Acceptance:** `grep -n \"refill\\|1e-4 \\*\\|max(V\" physics/thermo/accumulator.py` is clean of the fudge/clamps. A nominal run either completes with a physical trajectory or stops on the WARNING event with `ACCUMULATOR_NEAR_EMPTY` recorded. Confirm the WARNING event is registered in **both** `LumpedDigitalTwin.run` and `Network1DTwin` (P1 claimed this — verify it).\n\n---\n\n## P-B — Purge default-to-benign and arbitrary constants from the thermo layer\n\n`accumulator.py` (and you must audit the rest of `physics/thermo/`) is full of `getattr(config, 'p_pre', 1e5)`, `getattr(config, 'V_pre', 0.5)`, `getattr(config, 'eta_hydro', 0.8)`, `getattr(config, 'k_stiffness', 10.0)`, `getattr(config, 'plasma_vol', 0.05)`, `getattr(config, 'gas_mass', 0.1)` — the exact anti-pattern Phase 1 banned, plus hardcoded `C_v`, `A_piston`, `m_eff`.\n\n**Do:**\n1. Promote every one of these to an **explicit field in `config/system_config.py`** with a value that is either sourced or tagged `# PROVISIONAL`. Missing config → explicit error, never a silent numeric default.\n2. Provenance-tag the module honestly: `accumulator.py` is currently `# MODULE-STATUS: SCAFFOLD`, but it ships hardcoded constants that flow into `V_accum` dynamics. Mark it `PLACEHOLDER-PHYSICS` until its constants are sourced, and ensure any gate consuming its output stays INDETERMINATE per the provenance rule.\n3. `grep -rn \"getattr(config\" physics/` and resolve every hit under `physics/thermo/`.\n\n**Acceptance:** `grep -rn \"getattr(config\" physics/thermo/` returns nothing; the P-E lint (below) flags none of these; `system_config.py` carries the fields with PROVISIONAL tags.\n\n---\n\n## P-C — Fix the two real, currently-failing tests (and actually commit them this time)\n\n1. **`tests/physics/test_accumulator.py::test_pressure_relaxation`** — the pushed test still asserts `dV > 0` for `p_vessel = 1.1·p_accum`. The corrected physics is: `dV_dt ∝ (p_accum − p_vessel)`, so `p_vessel > p_accum` ⇒ gas compresses ⇒ **`dV < 0`**. Update the assertion to `assert dV < 0` and fix the stale comment. This is the edit you lost in the rebase — redo it and **verify it is in `git show HEAD:tests/physics/test_accumulator.py`** after commit (L-D).\n2. **`tests/unit/test_state_vector.py::test_segment_array_round_trip`** — `to_array()` then `from_array(arr, has_segments=True)` returns `segment_currents = None`. The serializer contract is inconsistent: `to_array()` default now drops segments, but a caller asking `has_segments=True` gets nothing back. Fix so `to_array(has_segments=True)` emits the segment block and `from_array(..., has_segments=True)` reconstructs it symmetrically — OR remove segment fields from persisted state and mark them derived. Then add the round-trip invariant as a real test (see P-D #7).\n\n**Acceptance:** both tests pass; the round-trip is provably symmetric.\n\n---\n\n## P-D — Build the REAL self-test suite (the one P2 was supposed to be)\n\n`tests/harness_selftest/test_harness.py` currently contains trivial `GateResult`-field and state-machine tests. Those are plumbing checks; they do not prove the judge can catch impossible physics. **Replace/augment with the seven falsification fixtures**, each asserting **verdict AND reason**:\n\n1. **σ = 0 → generation gate FAIL**, reason contains `\"no conductivity\"`. Not a crash, not INDETERMINATE.\n2. **claimed `W_net > X_source` → energy/exergy gate FAIL**, with the **hand-computed** imbalance asserted numerically.\n3. **divergent `dydt` → INDETERMINATE** via `IntegrationDivergedError` (P3 is wired — exercise it end-to-end through a gate).\n4. **`T_core < 0` injected mid-run → INDETERMINATE.** NOTE: `_run_twin_safely` currently catches only `IntegrationDivergedError`; a `NonPhysicalStateError` would escape as an uncaught test error. Fix the wrapper (P-F) so this maps to INDETERMINATE, then assert it here.\n5. **`T_core → 3695 K` → FAULT_LATCH**, reason `ELECTRODE_MELTDOWN`.\n6. **gate with a missing audited metric → INDETERMINATE** naming the metric. Not PASS.\n7. **round-trip invariant** `from_array(to_array(s)) == s` for random valid `s`, with and without segments (ties to P-C #2).\n\nKeep the existing plumbing tests if you like, but they do **not** count toward the seven. The bar for each fixture: *you must be able to watch it FAIL if the behavior regresses.* Demonstrate that by temporarily breaking the behavior, showing the fixture goes red, then restoring — and note it in the commit message.\n\n**Acceptance:** seven falsification fixtures exist, each asserting verdict + reason, all passing; and you have shown (in the commit message) that each can fail.\n\n---\n\n## P-E — Finish the CI lint (currently 1 of 5)\n\n`scripts/lint_tests.py` correctly AST-checks for `pytest.raises(Exception)` — good, keep it. But P5 required **five** checks. Add the other four:\n1. `getattr(...)` with a **numeric literal default** inside `physics/` or `validation/` (the accumulator getattr-defaults must be caught — that's the proof it works).\n2. `max(float(arr[...]), <literal>)` clamp patterns in `core/state_vector.py`.\n3. `GateResult(...)` constructed with a **boolean** verdict instead of a `Verdict` member.\n4. A scavenger status `EARNED`/`KILLED` paired with a `PLACEHOLDER`-tagged delta.\n\nRun it **before** P-B fixes: it must flag the accumulator getattr-defaults (proving the lint is live). After P-B, it passes. Wire all five into `ci.yml` ahead of `pytest`.\n\n**Acceptance:** the lint flags a deliberately-reintroduced instance of each pattern (demonstrate on a throwaway commit, then revert), and passes on the corrected tree.\n\n---\n\n## P-F — Make the gate run-wrapper handle non-physical states, not just divergence\n\n`validation/gates.py::_run_twin_safely` catches `IntegrationDivergedError` only. Post-declamp, a gate run can raise `NonPhysicalStateError` (e.g., V_accum ≤ 0) which currently escapes as an uncaught error, corrupting the gate result.\n\n**Do:** catch `NonPhysicalStateError` as well and map it to `Verdict.INDETERMINATE` with a reason like `f\"Non-physical state during run: {e}\"`. A gate must always return a `GateResult`, never throw.\n\n**Acceptance:** P-D fixture #4 passes through this path; no gate run can raise an uncaught exception.\n\n---\n\n## P-G — Real verification and honest reporting (P7, done properly this time)\n\n1. **Run the FULL suite.** Some dirs need optional deps (`h5py`, `zmq`/`pyzmq`, Qt/PyQt6). Either install them or, if you skip a dir, **state exactly which dirs were skipped and why** — do not silently drop them and call the remainder \"green.\" Paste the `pytest -rf -q` tail with real counts.\n2. **Categorize every non-pass** as exactly one of: real FAIL (fix now), `xfail(strict=True)` known-pending-physics (with a reason naming what's missing), or INDETERMINATE-by-design. Put the xfail table in `docs/MODULE_STATUS.md`.\n3. **Report git state accurately.** Run and paste `git ls-remote origin refs/heads/main`, `git rev-parse HEAD`, and `git log origin/twin/phase2-physical-accuracy --oneline -8`. State plainly: branch pushed = yes/no, PR open = #, **merged = only if main's SHA equals this HEAD.** Do not use the word \"merged\" otherwise.\n4. **Do not merge to `main`.** Leave PR #9 open for the human to merge. Merging requires: suite green except explicit xfails, the seven self-tests passing, the lint passing, and human go-ahead.\n5. **L-D self-check:** after the final push, re-read the pushed tree (`git show HEAD:<file>` for each file you claim to have changed) and confirm your summary matches what is actually on the remote. If anything you claimed is missing (as happened with the accumulator test last time), fix and re-push before reporting done.\n\n**Definition of Done for Phase 2:** accumulator fudge and module clamps gone; nominal run completes or terminates on the WARNING event with a reason; the two failing tests fixed and verified on the remote; seven real self-test fixtures passing; five-check lint live and passing; gate wrapper handles non-physical states; full-suite counts pasted with every non-pass categorized; git state reported truthfully with PR #9 left open. Only then is the twin ready for Phase 3 (real MHD physics).\n\n---\n\n## Doctrine reminders\n- The meter is the master. No number may be set to produce a desired state or verdict — that includes \"refill\" terms that exist to keep a variable in range.\n- Prefer INDETERMINATE over PASS. The harness abstaining is safe; the harness lying is not.\n- When the harness says the twin is broken, that is the harness working. Fix the twin.\n- Report only what the remote actually contains. Verify before you claim.\n</USER_REQUEST>\n<ADDITIONAL_METADATA>\nThe current local time is: 2026-07-21T05:47:31-06:00.\n</ADDITIONAL_METADATA>
